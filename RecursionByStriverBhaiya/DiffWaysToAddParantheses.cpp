#include<bits/stdc++.h>
using namespace std;
vector<int> diffWaysToCompute(string expression) {
    vector<int> ans;
    solve()
}
int main() {
    string s ;
    cin>>s;
    vector<int> ans = diffWaysToCompute(s);
    return 0;
}